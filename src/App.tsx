import React from 'react';
import { Header, Main } from './components';

const App: React.FC = () => {
    return (
        <div>
            <Header />
            <Main />
        </div>
    );
};

export default App;
